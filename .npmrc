; Exact-pin every dependency we add — no caret ranges. Keeps package.json and the
; lockfile in lockstep so a resolved version can't skew across installs (the
; vitest / @vitest/coverage-v8 pairing in particular must stay identical).
save-exact=true
