node_modules/
/dist
.check/
.fallow/
.stryker-tmp/
stryker.incremental.json
reports/
coverage/
*.log
.DS_Store
.claude/settings.local.json
.pnpm-store/
tsconfig.tsbuildinfo
.env

# NOTE: the plumbbob control plane is NOT ignored here. Per D33 those patterns
# live in the shared gitdir's info/exclude, written by `start`/`doctor` — see
# docs/decisions.md#d33. Nothing plumbbob-specific belongs in this tracked file.

research/
