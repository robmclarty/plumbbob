id: no-class
language: typescript
severity: error
message: 'C1: functional/procedural only — no classes, no `this`. Use plain functions and modules.'
files:
  - 'src/**/*.ts'
  - 'test/**/*.ts'
rule:
  any:
    - kind: class_declaration
    - kind: class
    - kind: this
